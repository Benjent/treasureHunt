/**
 * Class representing a mountain.
 * @extends Coordinates
 */
class Mountain extends Coordinates {
	/**
	 * Create a mountain.
	 * @constructor
	 * @param {number} x - The horizontal axis coordinate.
	 * @param {number} y - The vertical axis coordinate.
	 */
	constructor(x, y) {
		super(x, y);
	}
}